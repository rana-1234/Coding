#include <bits/stdc++.h>
using namespace std;

// *******************  Important Defines *****************************

#define MOD 1000000007
#define FAST_IO                \
  ios::sync_with_stdio(false); \
  cin.tie(NULL);               \
  cout.tie(NULL)
#define DEBUG_MODE false
#define _pub push_back
#define _puf push_front
#define _pob pop_back
#define _pof pop_front
#define _pu push
#define _po pop
#define _sz size
#define _fst first
#define _snd second
#define _ub upper_bound
#define _lb lower_bound
#define FOR(a, b, c) for (int i = (a); i < (b); i += (c))
#define RFOR(a, b, c) for (int i = (a); i > (b); i -= (c))
#define FOR_ALL_TEST \
  int t;             \
  cin >> t;          \
  while (t--)

// **************************** Important typedefs ******************************

typedef long long int _ll;
typedef unsigned long long int _ull;
typedef vector<_ll> _vll;
typedef vector<_ull> _vull;
typedef vector<_vll> _vvll;
typedef vector<_vull> _vvull;
typedef set<_ll> _sll;
typedef set<_ull> _sull;
typedef queue<_ll> _qll;
typedef queue<_ull> _qull;
typedef stack<_ll> _stll;
typedef stack<_ull> _stull;
typedef deque<_ll> _dll;
typedef deque<_ull> _dull;
typedef greater<_ll> _gll;
typedef greater<_ull> _gull;
typedef less<_ll> _lll;
typedef less<_ull> _lull;
typedef priority_queue<_ll> _pqll;
typedef priority_queue<_ull> _pqull;
typedef priority_queue<_ll, _vll, _gll> _pqgll;
typedef priority_queue<_ull, _vull, _gull> _pqgull;
typedef multiset <_ll> _msll;
typedef multiset <_ull> _msull;
typedef multiset <_ll, _gll> _msgll;
typedef map<_ll, _ll> _mll_ll;
typedef map<_ull, _ull>  _mull_ull;
typedef multimap <_ull, _ull> _mmull_ull;
typedef multimap <_ll, _ll> _mmll_ll;

// ***************************** End of diffs ************************************

int main()
{
  // Chal yaar suru kar de code !!

  FAST_IO;
}
